id:
transport: http
rules:
  r0:
    request:
      cache: true
      follow_redirects: true
      method: GET
      path: /
    expression: response.title.icontains("Test Page for the HTTP Server on Fedora")
expression: r0()
detail:
  fingerprint:
    product: default-fedora-page