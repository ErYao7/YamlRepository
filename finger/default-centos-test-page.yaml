id:
transport: http
rules:
  r0:
    request:
      cache: true
      follow_redirects: true
      method: GET
      path: /
    expression: response.title.icontains("HTTP Server Test Page powered by CentOS-WebPanel.com")
expression: r0()
detail:
  fingerprint:
    product: default-centos-test-page