id:
transport: http
rules:
  r0:
    request:
      method: GET
      path: /
    expression: response.status == 200 &&  response.title.icontains("RocketMq-console-ng")
expression: r0()
detail:
  fingerprint:
    product: rocketmq-console-exposur